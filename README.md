# catchall
a super easy Docker-Compose file provisioning server built on nginx


Make sure to either add these to .env or specify values
"
CATCHALL_HOST=files.example.com
MANAGER_HOST=filebrowser.example.com
"